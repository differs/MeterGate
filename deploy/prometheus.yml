global:
  scrape_interval: 5s
  evaluation_interval: 10s
rule_files:
  - /etc/prometheus/alerts.yml
scrape_configs:
  - job_name: metergate
    static_configs:
      - targets: ["host.docker.internal:3000"]
